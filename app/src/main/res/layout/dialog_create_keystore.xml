<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@color/bg_card">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Generate RSA 2048-bit Keystore"
        android:textColor="@color/text_primary"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Creates a production-ready JKS signing keystore on the server with keytool."
        android:textColor="@color/text_muted"
        android:textSize="12sp"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="14dp" />

    <EditText
        android:id="@+id/editKeystoreAlias"
        android:layout_width="match_parent"
        android:layout_height="42dp"
        android:background="@drawable/bg_input_field"
        android:hint="Key Alias (e.g. release_key)"
        android:textSize="13sp"
        android:paddingHorizontal="10dp"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/editKeystorePassword"
        android:layout_width="match_parent"
        android:layout_height="42dp"
        android:background="@drawable/bg_input_field"
        android:hint="Keystore Password"
        android:inputType="textPassword"
        android:textSize="13sp"
        android:paddingHorizontal="10dp"
        android:layout_marginBottom="14dp" />

    <Button
        android:id="@+id/btnSubmitCreateKeystore"
        style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:text="Generate Keystore"
        android:backgroundTint="@color/primary"
        android:textColor="#FFFFFF"
        app:cornerRadius="10dp" />
</LinearLayout>
